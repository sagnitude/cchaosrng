#ifndef common__
#define common__
#include "utils.h"
typedef struct ChaosPool ChaosPool;
typedef struct Point Point;
typedef struct Task Task;
typedef struct TaskQueue TaskQueue;
typedef struct MutableDoubleArray MutableDoubleArray;
typedef int bool;
#define true 1
#define false 0
#define private static
#endif
